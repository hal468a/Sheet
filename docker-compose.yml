version: '3.8'
services:
  sheet:
    image: openjdk:11
    container_name: sheet_service
    volumes:
      - ./src:/src
    working_dir: /src
    command: >
      bash -c "chmod +x activate.sh && 
              ./activate.sh"